; Instruments can be assigned to a MIDI channel (1-16) by including
; the instrument name in the array below. The instrument name's index
; is the channel to which it will be assigned.

gSMidiChannelAssignments[] fillarray ""

#include "config/assignMidiChannels.orc"
